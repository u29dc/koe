{
	"name": "koe",
	"version": "0.0.1",
	"type": "module",
	"private": true,
	"repository": {
		"type": "git",
		"url": "https://github.com/u29dc/koe"
	},
	"scripts": {
		"init---------------------------------": "",
		"prepare": "husky",
		"build--------------------------------": "",
		"build": "cargo build --workspace --release",
		"koe": "./target/release/koe",
		"check--------------------------------": "",
		"util:format": "cargo fmt --all",
		"util:lint": "cargo clippy --all-targets --all-features -- -D warnings",
		"util:test": "cargo test --all",
		"util:check": "STATUS=0; bun run util:format || STATUS=1; bun run util:lint || STATUS=1; bun run util:test || STATUS=1; exit $STATUS",
		"util:clean": "cargo clean"
	},
	"devDependencies": {
		"@commitlint/cli": "^20.4.0",
		"@commitlint/config-conventional": "^20.4.0",
		"husky": "^9.1.7",
		"lint-staged": "^16.2.7"
	}
}
